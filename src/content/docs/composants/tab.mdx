---
title: Tab
description: Onglet Ã  soulignement â€” actif = underline violet 2px + glow.
---

import Tab from '../../../components/clutch/Tab.tsx';

Onglet (pattern **soulignement**). Actif = border-bottom violet 2px + drop-shadow violet, texte encre. Inactif = texte ink 40 %, pas de bordure. Le pill violet est dÃ©prÃ©ciÃ©.

## AperÃ§u live

<div style={{ display: 'flex', gap: '4px', alignItems: 'center', padding: '24px 24px 0', background: '#1a191c', borderBottom: '1px solid rgba(255,253,250,.1)' }}>
  <Tab label="Toutes" count={47} active client:load />
  <Tab label="Ouvertes" count={12} client:load />
  <Tab label="En cours" count={3} client:load />
  <Tab label="TerminÃ©es" count={32} client:load />
</div>

## Specs

| PropriÃ©tÃ© | Valeur                                   |
| --------- | ---------------------------------------- |
| Padding   | H 16px                                   |
| Texte     | Space Grotesk Bold 11px Â· LS 0.5px Â· UPPERCASE |
| Actif     | border-bottom 2px violet + drop-shadow violet |
| Inactif   | texte ink 40 % Â· pas de bordure          |
| Radius    | 0                                        |

## Props

```ts
label: string
count?: number
active?: boolean
onClick?: () => void
```

## Usage

Format de libellÃ© : `TOUTES Â· 47`. Un seul onglet actif Ã  la fois ; gÃ©rer l'Ã©tat actif au niveau du conteneur parent.
